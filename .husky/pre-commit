#!/usr/bin/env sh

echo "🔍 Running pre-commit checks..."

# TypeScript type check
npx tsc --noEmit --pretty 2>&1 | head -20
if [ ${PIPESTATUS[0]} -ne 0 ]; then
  echo "❌ TypeScript 类型检查未通过，请修复后重新提交"
  exit 1
fi

# Lint staged files
npx lint-staged
